const {createSession,setSessionCookie,verifyPassword,noStore}=require('../../lib/auth');
module.exports=async(req,res)=>{noStore(res);if(req.method!=='POST')return res.status(405).json({error:'METHOD_NOT_ALLOWED'});try{const user=process.env.SNOO_ADMIN_USERNAME,hash=process.env.SNOO_ADMIN_PASSWORD_HASH;if(!user||!hash)return res.status(500).json({error:'ADMIN_NOT_CONFIGURED'});const b=req.body||{};if(String(b.username||'')!==user||!(await verifyPassword(b.password,hash)))return res.status(401).json({error:'INVALID_CREDENTIALS'});setSessionCookie(res,createSession(user));return res.status(200).json({ok:true})}catch(e){console.error('admin login failed',e.message);return res.status(500).json({error:'LOGIN_FAILED'})}};
